import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

const playfair = Playfair_Display({
  variable: "--font-playfair",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "LexAI - Legal Research Assistant",
  description: "AI-powered legal research assistant for comprehensive legal analysis, case law research, and statutory interpretation. Get expert legal insights with proper citations and professional guidance.",
  keywords: ["legal research", "AI legal assistant", "case law", "legal analysis", "court decisions", "statutory interpretation"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${inter.variable} ${playfair.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
